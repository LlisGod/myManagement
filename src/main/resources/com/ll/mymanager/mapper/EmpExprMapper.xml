<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ll.mymanager.mapper.EmpExprMapper">


    <insert id="insertBatch">
        insert into work_experience (emp_id, company, start_date, end_date) values
        <foreach collection="empExprs" item="empExpr" separator=",">
            (#{empExpr.empId},#{empExpr.company},#{empExpr.startDate},#{empExpr.endDate})
        </foreach>
    </insert>
    <delete id="deleteBatchEmpIds">
        delete from work_experience where emp_id in
        <foreach item="empId" collection="ids" separator="," close=")" open="(">
            #{empId}
        </foreach>
    </delete>
</mapper>